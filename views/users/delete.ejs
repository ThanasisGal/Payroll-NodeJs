<div
  class="modal fade"
  id="delModal"
  data-bs-backdrop="static"
  data-bs-keyboard="false"
  tabindex="-1"
  aria-labelledby="staticBackdropLabel"
  aria-hidden="true"
>
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header bg-danger">
        <div class="modal-title fw-bold" style="font-size: 18px"><i class="bi bi-trash3"></i>
          Διαγραφή Χρήστη
        </div>
      </div>

      <div class="modal-body">
        <p>
          Ο χρήστης
          <b class="fw-bold"><%= users.lastName %> <%= users.firstName %></b> και με ID <b class="fw-bold"><%= users._id %></b> θα διαγραφεί οριστικά.<br /><br />
          Είστε σίγουρος(η);
        </p>
      </div>
      <div class="modal-footer">
        <form action="/admin" method="GET">
          <button
            type="submit"
            class="btn btn-outline-secondary"
            data-bs-dismiss="modal">Κλείσιμο
          </button>
        </form>

        <form
          action="/admin/edit/<%= users._id %>?_method=DELETE"
          method="POST"
          class="position-relative"
        >
          <button type="submit" class="btn btn-outline-danger">
            Διαγραφή Χρήστη
          </button>
        </form>
      </div>
    </div>
  </div>
</div>
